{
  "name": "@post-image-builder/root",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/application",
    "packages/ui"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@post-image-builder/application",
    "build": "npm run build --workspace=@post-image-builder/application",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "type-check": "npm run type-check --workspaces"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
